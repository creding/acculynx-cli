const ContactState = {
  "type": "object",
  "properties": {
    "id": {
      "type": "integer",
      "description": "The id for the state.",
      "examples": [
        43
      ]
    }
  },
  "title": "contactState",
  "x-readme-ref-name": "contactState"
} as const;
export default ContactState
